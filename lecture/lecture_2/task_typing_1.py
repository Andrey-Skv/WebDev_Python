# a: int = 4
# b: float = float(input('Введите число: '))
# a = a / b  # подчеркивает, так как задали в перовой строке, что переменная это целое число
# # но т.к. динамическая типизация, то программа сработает


a: float = 4.0
b: float = float(input('Введите число: '))
a = a / b